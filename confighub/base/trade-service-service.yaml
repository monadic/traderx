apiVersion: v1
kind: Service
metadata:
  name: trade-service
  namespace: traderx-dev
spec:
  selector:
    app: trade-service
  ports:
  - port: 18092
    targetPort: 18092
    protocol: TCP
  type: ClusterIP