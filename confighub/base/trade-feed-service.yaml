apiVersion: v1
kind: Service
metadata:
  name: trade-feed
  namespace: traderx-dev
spec:
  selector:
    app: trade-feed
  ports:
  - port: 18086
    targetPort: 18086
    protocol: TCP
  type: ClusterIP