#!/bin/bash
# Returns the project name for use in other scripts

if [ -f .cub-project ]; then
  cat .cub-project
else
  echo "ERROR: .cub-project file not found. Run bin/install-base first." >&2
  exit 1
fi